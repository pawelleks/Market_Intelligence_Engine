[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mie_lib"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "src"}
packages = {find = {where = ["src"]}}

