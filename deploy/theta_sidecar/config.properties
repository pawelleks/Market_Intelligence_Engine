# Theta Terminal Configuration
# Enable binding to all interfaces for Docker access
bind.address=0.0.0.0
# Explicitly set ports (though defaults should apply)
server.port=11000
server.streaming.port=11001
server.streaming.bind.address=0.0.0.0
